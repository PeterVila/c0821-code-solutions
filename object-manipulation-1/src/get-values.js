/* exported getValues */
function getValue(object, key){
    return object[key];
}

/*
Inputs: Taking in an object, and a name
Output: Looking for the name within the object

Return the key value within the object.
*/
