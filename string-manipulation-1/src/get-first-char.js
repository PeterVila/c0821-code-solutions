/* exported getFirstChar */

// getFirstChar
// input: String(containing a word)
// output: String(first letter of the string input)
// Look at only the first index of a string which is the 0 position
// return that value

function getFirstChar(string) {
  return string[0];
}
