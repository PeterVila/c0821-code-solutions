/* exported last */

function last(array) {
  var length = array.length;
  return array[length - 1];
}

/*
Input: a list of items
Output: the last item of the list

Create a valeu which equals the length of the input
Return the last value;
*/
