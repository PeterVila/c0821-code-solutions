/* exported head */

function head(array) {
  var firstArr = [];
  firstArr.push(array[0]);
  return array[0];
}

// Input: a list
// Output: the first value in the list
// Create a new storage
// Push the first value of the input list onto the new storage
// return the new storage
